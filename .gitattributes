# Normalize line endings to LF in the repo regardless of a contributor's local git config
# (autocrlf), so diffs stay clean across Windows/non-Windows checkouts.
* text=auto eol=lf

*.png binary
*.ico binary
